import csv
import random
from datetime import datetime, timedelta

def generate_sales_data(rows=100):
    """Generates sample sales data."""
    data = []
    data.append(["OrderID", "Product", "Quantity", "Price", "Date"])  # Header

    products = ["Laptop", "Keyboard", "Mouse", "Monitor", "Webcam", "Printer", "Scanner", "Router", "External Hard Drive", "USB Drive"]

    start_date = datetime.now() - timedelta(days=365)

    for i in range(1, rows + 1):
        order_id = f"ORD{i:05d}"
        product = random.choice(products)
        quantity = random.randint(1, 10)
        price = round(random.uniform(10.0, 1000.0), 2)
        date = (start_date + timedelta(days=random.randint(0, 365))).strftime("%Y-%m-%d")
        data.append([order_id, product, quantity, price, date])

    return data

def create_csv_file(filename="sales_data.csv", data=None):
    """Creates a CSV file from data."""
    if data is None:
        data = generate_sales_data()

    with open(filename, 'w', newline='') as csvfile:
        writer = csv.writer(csvfile)
        writer.writerows(data)

if __name__ == "__main__":
    sales_data = generate_sales_data(100)
    create_csv_file("sales_data.csv", sales_data)
    print("sales_data.csv created successfully with 100 rows.")